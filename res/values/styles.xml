<resources>

    <style name="AppTheme" parent="android:Theme.Light" />
    
    <!-- ListView -->
    <style name="ShopListViewStyle" parent="android:Widget.ListView">
        <!-- 指定分割线颜色 -->
        <item name="android:divider" >#DCDCDC</item>
        <!-- 指定分割线高度 -->
        <item name="android:dividerHeight" >1px </item>
         <!-- 设置透明背景色 -->
        <item name="android:cacheColorHint" >#00000000</item>
        
    </style >
    
    <!-- RatingBar -->
    <style name="ShopRatingBarStyle" >
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable" >@drawable/ratingbar</item>
        <item name="android:indeterminateDrawable" >@drawable/ratingbar</item>        
        <item name="android:minHeight" >25px </item>
        <item name="android:maxHeight" >25px </item>
        <item name="android:thumb">@null</item>
    </style>
    
    <style name="myDialogTheme">
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
    </style>
    
</resources>
<resources>

    <style name="AppTheme" parent="android:Theme.Light" />
    
    <!-- ListView -->
    <style name="ShopListViewStyle" parent="android:Widget.ListView">
        <!-- 指定分割线颜色 -->
        <item name="android:divider" >#DCDCDC</item>
        <!-- 指定分割线高度 -->
        <item name="android:dividerHeight" >1px </item>
    </style >
    
    <!-- RatingBar -->
    <style name="ShopRatingBarStyle" parent="android:Widget.RatingBar">
        <item name="android:progressDrawable" >@drawable/ratingbar</item>
        <item name="android:indeterminateDrawable" >@drawable/ratingbar</item>        
        <item name="android:minHeight" >22px </item>
        <item name="android:maxHeight" >22px </item>
        <item name="android:isIndicator" >true</item>
    </style>
    
    <style name="myDialogTheme" parent="android:Theme.Dialog">
        <item name="android:windowBackground">@color/transparent</item>
    </style>
    
</resources>